/// 정렬된 단위 토큰 한 개의 상태.
enum WordStatus {
  /// 정확히 일치하거나 유사도가 매우 높은 토큰.
  match,

  /// 다른 발음으로 잘못 말한 경우 (치환).
  mismatch,

  /// 목표에 없는데 추가로 말한 토큰 (삽입).
  extra,

  /// 목표에 있는데 빠뜨린 토큰 (삭제).
  missing,

  /// 한자 ↔ 가나 표기 차이로 인해 발음 일치 여부를 판단할 수 없는 토큰.
  unknown,
}
